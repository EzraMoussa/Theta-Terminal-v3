services:
  thetaterminal-v3:
    build: .
    container_name: thetaterminal-v3
    restart: unless-stopped
    environment:
      - THETA_V3_USER
      - THETA_V3_PASS
    ports:
      - "25503:25503"
    networks:
      - thetanet

networks:
  thetanet:
    driver: bridge